function getDifferentNumber(inputAray) {
    var length = inputAray.length;
    var map = new Map();
    var index;
    for (index = 0; index < length; index++) {
        map.set(index, true);
    }
}
module.exports = getDifferentNumber;
